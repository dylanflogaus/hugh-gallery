# Copy to .dev.vars for `npm run dev` (Worker + D1 local).
# Never commit .dev.vars.
ADMIN_API_SECRET=dev-change-me-to-a-long-random-string
# Optional: Stripe test secret for checkout (sk_test_…)
# STRIPE_SECRET_KEY=
